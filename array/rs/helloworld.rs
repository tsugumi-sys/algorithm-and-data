fn main() {
    println!("Hello World!") // println = print line (line break), ! means calling macro
}

// Macro is a construct that generates or expand into code at compile time. A function runs at runtime, while a macro is expanded at compile time.
